# customer-revenue-pipeline

Bronze → Silver → Gold transformations for the EMEA customer revenue mart.

- `pipelines/customer_transform.py` — transformation logic executed by the pipeline runner
- `tests/` — transformation regression tests (`python -m pytest tests`)

This repository is created by DataSentinel's demo reset and is the codebase
DataSentinel monitors and investigates.
